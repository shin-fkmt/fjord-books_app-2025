<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('common.link_label.edit'), edit_book_path(@book) %> |
  <%= link_to t('common.link_label.index'), books_path %>

  <%= button_to t('common.button_label.destroy'), @book, method: :delete %>
</nav>
