<% content_for :title, t('.title') %>

<h1><%= t('.title') %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('common.link_label.show'), @book %> |
  <%= link_to t('common.link_label.index'), books_path %>
</nav>
