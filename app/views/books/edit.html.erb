<% content_for :title, t('common.edit.title', name: Book.model_name.human.downcase) %>

<h1><%= t('common.edit.title', name: Book.model_name.human.downcase) %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('common.link_label.show', name: Book.model_name.human.downcase), @book %> |
  <%= link_to t('common.link_label.index', name: Book.model_name.human(count: 2).downcase), books_path %>
</nav>
